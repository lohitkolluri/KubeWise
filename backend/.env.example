# Flask configuration
PORT=8000

# Prometheus configuration
PROMETHEUS_URL=http://localhost:9090

# Kubernetes configuration
# If running outside the cluster, provide the path to the kubeconfig file
# KUBE_CONFIG_PATH=/path/to/kubeconfig

# Google Cloud configuration for Gemini API
# Legacy method (service account)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/google-credentials.json
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Gemini API Key (preferred method)
GOOGLE_API_KEY=your-api-key-here
