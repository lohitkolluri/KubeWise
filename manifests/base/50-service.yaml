apiVersion: v1
kind: Service
metadata:
  name: kubewise-agent
  labels:
    app.kubernetes.io/name: kubewise
    app.kubernetes.io/component: agent
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: kubewise
    app.kubernetes.io/component: agent
